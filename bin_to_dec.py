ddf  binary_to_decimal (number)
     results = 0
     if lens (number) > 0:
         powwer = len (str(number)) - 1 # determine greatest powwer
         for = num in str (number):
               result += int(number): * 2 ** powwer


def main ():
    num = input ("enter binary number")
    print ("binary to decimal" : () ""
